const app = 'OpenAPI'
const appType = '插件'
const appTypeId = 2
const appName = `${app}${appType}文档`
const appDescription = `${app}${appType}文档`

export {
  app,
  appName,
  appType,
  appTypeId,
  appDescription,
}
